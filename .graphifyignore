# Exclude the UVKBuildTool C++ submodule and its vendored dependencies
# (rapidyaml/c4core) from the graphify knowledge graph — they're third-party
# build-tool internals, not Youyin source.
UVKBuildTool/
